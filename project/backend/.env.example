# Backend Environment Configuration
# Copy this file to .env and update values for your environment

# Server Port
PORT=8000

# MongoDB Connection String
# For production, use MongoDB Atlas or your production MongoDB instance
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/foodiesapp

# JWT Secret Key (IMPORTANT: Use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Redis URL (for caching and rate limiting)
REDIS_URL=redis://127.0.0.1:6379

# Optional: For Cloud Deployments
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com
